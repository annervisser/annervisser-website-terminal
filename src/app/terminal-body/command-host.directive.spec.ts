import {CommandHostDirective} from './command-host.directive';

describe('CommandHostDirective', () => {
    it('should create an instance', () => {
        const directive = new CommandHostDirective();
        expect(directive).toBeTruthy();
    });
});
